import { useState } from "react";

const World = () => {
  const [world, setWorld] = useState(null);
  const [isLoading, setIsLoading] = useState(true);
  return ( <div>Welcome to the world page</div> );
}
 
export default World;<div>Welcome to the world page</div>